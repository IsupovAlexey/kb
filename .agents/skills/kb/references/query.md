# Query workflow

**Stage 3** — not wired in Stage 2. Placeholder for progressive discovery layout.

When implemented: qmd top-N retrieval over `wiki/`, read ranked pages only, cited synthesis in chat. Optional file-back when user asks. No commit by default.

See `openspec/changes/init-llm-wiki/design.md` Decision 9 and `specs/kb-search/spec.md`.
