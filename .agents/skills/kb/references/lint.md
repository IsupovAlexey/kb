# Lint workflow

**Stage 3** — not wired in Stage 2. Placeholder for progressive discovery layout.

When implemented: run `npm run kb:lint` (semantic wiki-lint) and `npm run format:check` (Prettier). Report CLI output — not an LLM file-by-file checklist.

See `openspec/changes/init-llm-wiki/specs/kb-lint/spec.md` and Stage 3 tasks in `openspec/changes/init-llm-wiki/tasks.md`.
