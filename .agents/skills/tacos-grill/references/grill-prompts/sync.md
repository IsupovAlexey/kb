# Sync triggered grill

Triggered grill only (no gather step). Focus when delta→main spec merge is ambiguous:

- Conflicting requirement semantics between delta and main spec
- Whether to merge, replace, or split requirements
- Migration or archive implications

Run only when triggered during **sync**.
