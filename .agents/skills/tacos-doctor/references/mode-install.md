# Mode: install

Load when user invokes `/tacos-doctor install`, requests install, or workspace install.

Layout: [workspace-install.md](workspace-install.md). Full flow: [install.md](install.md).

Bootstrap when `check-prereqs.cs` exits `2` or `3`: [check-prereqs.md — OpenSpec bootstrap](check-prereqs.md#openspec-bootstrap).

## Done when

Per [install.md — Done when](install.md#done-when).
