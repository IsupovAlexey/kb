# When orchestration is off

If `orchestration.enabled` is false, follow stock opsx only. Manual `/tacos-grill` and user-invocable `/tacos-spec-review`, `/tacos-apply-review` still work when installed.
