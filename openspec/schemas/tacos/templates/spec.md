<!-- Behavioral contracts only: durable positive obligations (SHALL/MUST), not inventories, runbooks, or exclusion laundry. Title requirements by what the system does—not "No X" / "Without Y". Grill non-goals → design Boundaries; not one spec requirement per grill bullet. MUST NOT: one inline guardrail per positive requirement only. Scenarios: positive observable outcomes—not absence-only THEN (no files, did not run). Few broad requirements; one invariant scenario per branch. Design Boundaries ≤3 brief lines. No TBD — design Open Questions. Before write: main-spec cross-check per schema specs instruction — STOP and update proposal Modified Capabilities when delta contradicts unlisted capability; for multi-step flows add failure/unavailable-dependency/partial-completion scenarios (implicit branches). -->

## ADDED Requirements

### Requirement: <!-- requirement name -->

<!-- Concrete requirement text (no TBD/TODO). Anchor new/non-trivial behavior to an existing capability, module, API surface, or pattern. -->

#### Scenario: <!-- scenario name -->

- **WHEN** <!-- condition -->
- **THEN** <!-- expected outcome -->
