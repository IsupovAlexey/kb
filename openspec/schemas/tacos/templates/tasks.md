## 1. <!-- Task Group Name -->

**Testable outcome:** <!-- One sentence: how you will know this stage succeeded (e.g. command passes, file exists, behavior verified) -->

- [ ] Stage grill: per task-stage-contract.md ## Stage grill line — **this stage only** (`## 1` + unchecked below); **User inputs** under `## apply` ### Stage 1 in `grill-summaries.md`
- [ ] 1.1 <!-- Verb-led terse path/command + done-when; cite Requirement title or Decision N — no slop wrappers (artifact-prose.md) -->
- [ ] 1.2 <!-- Verb-led terse path/command + done-when; cite Requirement title or Decision N — no slop wrappers (artifact-prose.md) -->
- [ ] Verify Decision N: <!-- match design **Decision N** (not stage index); add multiple rows when one stage verifies more than one decision; omit when none require verification -->
- [ ] Tests: N/A — <!-- one-line reason when no behavior change; or explicit test tasks when behavior changes -->
- [ ] Apply review: parallel Task — `tacos-apply-review` + `tacos-additional-apply-review` when applicable (parent merge; **MUST NOT** core-only); write `artifacts/openspec-reviews/<change>/apply-review-1.md`
- [ ] Human review: Pause for human sign-off before stage 2

## 2. <!-- Task Group Name -->

**Testable outcome:** <!-- One sentence: how you will know this stage succeeded -->

- [ ] Stage grill: per task-stage-contract.md ## Stage grill line — **this stage only** (`## 2` + unchecked below); **User inputs** under `## apply` ### Stage 2 in `grill-summaries.md`
- [ ] 2.1 <!-- Verb-led terse path/command + done-when; cite Requirement title or Decision N — no slop wrappers (artifact-prose.md) -->
- [ ] 2.2 <!-- Verb-led terse path/command + done-when; cite Requirement title or Decision N — no slop wrappers (artifact-prose.md) -->
- [ ] Verify Decision N: <!-- match design **Decision N** (not stage index); add multiple rows when one stage verifies more than one decision; omit when none require verification -->
- [ ] Tests: N/A — <!-- one-line reason when no behavior change; or explicit test tasks when behavior changes -->
- [ ] Apply review: parallel Task — `tacos-apply-review` + `tacos-additional-apply-review` when applicable (parent merge; **MUST NOT** core-only); write `artifacts/openspec-reviews/<change>/apply-review-2.md`
- [ ] Human review: Pause for human sign-off before the next stage (add `## 3.` … and matching `### Stage 3` in `grill-summaries.md` when more stages follow)
