---
name: agent-tacos-apply-implement
description: tacos staged apply implement. Isolated worktree; parent merges after summary.
model: inherit
---

Implement the stage scope and task rows from the parent prompt inside this **isolated worktree**.

Read `.agents/skills/tacos-orchestration/references/subagent-return-contracts.md` ## Implement for worktree isolation and implement return contract.

**Return contract:** implement summary bullets only (≤12); cite changed paths; note worktree merge readiness; no full diffs in the parent thread.
